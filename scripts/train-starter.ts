import { CHECKPOINT_SCHEMA_VERSION } from "../src/persistence/types";

console.log(
  `Starter training scaffold ready for checkpoint schema v${CHECKPOINT_SCHEMA_VERSION}. No files were written.`,
);
